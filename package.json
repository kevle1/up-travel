{
  "name": "up-travel",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev": "pnpm --parallel --filter './apps/*' dev"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "@types/node": "22.7.0"
  }
}
